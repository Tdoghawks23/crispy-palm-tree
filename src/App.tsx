/**
 * Placeholder root component.
 *
 * This is intentionally minimal — real screens (session view, progress view,
 * program overview) are PRD R15-R20 and are built in the next invocation.
 * This file exists only so the scaffold compiles and renders something.
 */
function App() {
  return (
    <main style={{ padding: '1rem', textAlign: 'center' }}>
      <h1>Arm Growth</h1>
      <p>Session screens coming soon.</p>
    </main>
  );
}

export default App;
